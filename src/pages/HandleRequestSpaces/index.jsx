import React from 'react';
import { Col, Row, Container, Card, CardImg, CardBody, CardTitle, CardText, Form, FormGroup, Label, Input, Button } from 'reactstrap';

const HandleRequestSpaces = () => {
    return (
        <>
            <Container>

            </Container>
        </>
    );
};

export default HandleRequestSpaces;